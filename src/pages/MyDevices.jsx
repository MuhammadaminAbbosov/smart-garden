import React from "react";
import styled from "styled-components";

const MyDevices = () => {
  document.title = "Smart Garden | My Devices";

  return <Wrapper>MyDevices</Wrapper>;
};

export default MyDevices;
const Wrapper = styled.div``;
